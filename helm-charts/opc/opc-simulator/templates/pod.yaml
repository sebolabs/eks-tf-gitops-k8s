apiVersion: v1
kind: Pod
metadata:
  name: {{ .Values.environment }}-busybox
  namespace: {{ .Values.environment }}-{{ .Values.parentApp }}
spec:
  containers:
  - image: busybox
    command:
      - sleep
      - "3600"
    imagePullPolicy: IfNotPresent
    name: busybox
  restartPolicy: Always
