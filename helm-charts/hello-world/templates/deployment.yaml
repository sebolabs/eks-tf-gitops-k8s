apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.environment }}-hello-world-deployment
  labels:
    app: {{ .Values.environment }}-hello-world
spec:
  selector:
    matchLabels:
      app: {{ .Values.environment }}-hello-world
  replicas: {{ .Values.replicas }}
