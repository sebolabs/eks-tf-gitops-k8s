apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.environment }}-hello-universe
spec:
  selector:
    app: {{ .Values.environment }}-hello-universe
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: {{ .Values.service.nodePort }}   
  type: NodePort
