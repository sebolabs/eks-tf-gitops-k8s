apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.environment }}-kirk
spec:
  selector:
    app: {{ .Values.environment }}-kirk
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: {{ .Values.service.nodePort }}   
  type: NodePort
