apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.environment }}-spock
spec:
  selector:
    app: {{ .Values.environment }}-spock
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: {{ .Values.service.nodePort }}   
  type: NodePort
