apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.environment }}-spock
  labels:
    app: {{ .Values.environment }}-spock
spec:
  selector:
    matchLabels:
      app: {{ .Values.environment }}-spock
  replicas: {{ .Values.replicas }}
  template:
    metadata:
      labels:
        app: {{ .Values.environment }}-spock
    spec:
      containers:
      - name: {{ .Values.environment }}-spock
        image: registry.k8s.io/e2e-test-images/agnhost:2.39
        ports:
        - containerPort: 80
        resources:
          limits:
            memory: 256Mi
            cpu: "250m"
          requests:
            memory: 128Mi
            cpu: "80m"
